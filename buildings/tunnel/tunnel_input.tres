[gd_resource type="Resource" script_class="BuildingDef" load_steps=3 format=3]

[ext_resource type="Script" path="res://buildings/shared/building_def.gd" id="1"]
[ext_resource type="PackedScene" path="res://buildings/tunnel/tunnel_input.tscn" id="2"]

[resource]
script = ExtResource("1")
id = &"tunnel_input"
display_name = "Tunnel"
color = Color(0.4, 0.5, 0.3, 1)
category = "tunnel"
description = "Underground transport. Place input, then output. Max 4 cells apart."
scene = ExtResource("2")
is_ground_level = true
build_cost = []
