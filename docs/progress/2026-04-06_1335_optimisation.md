# 2026-04-06 13:35 — Performance Optimisation Pass

**Duration**: ~47 minutes (afternoon)

## Work Done

Full performance audit of all GDScript files, followed by implementation of 19 out of 20 identified optimisations.

### Review Phase
- 3 parallel critic agents reviewed all .gd files grouped by system (core, buildings, UI/player)
- Compiled findings into `docs/optimisation/2026-04-06.md` — 20 items total

### Implementation (19 items)

**Transport (#1-4)**: Conveyors and splitters now skip `get_overlapping_bodies()` when their force zones are empty (body_entered/body_exited counter). Splitter caches ForceZone reference and output directions.

**Physics (#5, #6, #14)**: Disabled CCD on PhysicsItem (items are slow). Cached collision shapes per item type (skip vertex gathering on spawn). Replaced trimesh building collision with simple box shapes; flat buildings (conveyor, splitter, junction) skip collision entirely.

**Build System (#11-13)**: BuildSystem._process now skips raycast, ghost updates, and destroy highlights when the mouse hasn't moved (grid_changed check).

**Buildings (#7-10)**: BuildingTickSystem uses swap-and-pop. Converter caches InputZone refs. InputZone.get_items() returns array directly (body_exited handles cleanup). OutputZone caches CollisionShape3D.

**UI (#15-19)**: Non-browsable ItemIcons disable _process. GameWindow lookup uses Godot groups instead of tree traversal. HUD updates phase/round labels only on phase_changed signal, throttles FPS label to every 30 frames, removed global node_added listener.

### Deferred
- #20 Chunked terrain — LOW impact now, LARGE effort, only needed if terrain destruction becomes frequent

### Verification
- Parse check: clean
- Simulations: no regressions (failures are pre-existing RoundManager timing issues)
- Unit tests: pass

## Blockers
- sim_physics_transport and sim_stress_test fail on baseline due to fight-phase freezing items — needs separate investigation

## Next
- Investigate/fix simulation timing issues with RoundManager fight phase
