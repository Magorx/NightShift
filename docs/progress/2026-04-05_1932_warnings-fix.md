# 2026-04-05 Evening -- Fix GDScript Warnings

**Duration**: ~37 min (18:55 - 19:32)

## Work Done

- Fixed all 49 GDScript warnings reported by the Godot LSP / VSCode extension
- **STATIC_CALLED_ON_INSTANCE (~30)**: Removed `static` from `grid_utils.gd` and `logger.gd` functions -- they're always called via autoload instances, not class references
- **UNUSED_VARIABLE (3)**: Removed or prefixed unused locals in `player.gd`, `build_system.gd`, `building_popup.gd`
- **UNUSED_PRIVATE_CLASS_VARIABLE (3)**: Removed dead `_hovered_conv`, `_hovered_conv_item_idx`, `_is_grounded` from `player.gd`
- **UNUSED_PARAMETER (3)**: Prefixed unused params in `building_logic.gd`, `grid_utils.gd`
- **SHADOWED_VARIABLE (3)**: Renamed `map_size` param in `game_manager.gd`, fixed `_item_id` shadow in `item_icon.gd`
- Updated `test_grid_utils.gd` to instantiate GridUtils instead of calling static methods on preloaded script
- All 33 tests pass clean

## Blockers

None.

## Next

Continue with kanban board tasks (Phase 3: RoundManager).
