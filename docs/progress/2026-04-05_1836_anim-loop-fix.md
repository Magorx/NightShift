# Session: 3D Animation Loop Fix

**Date**: 2026-04-05 evening
**Duration**: ~3 min (18:36 – 18:39)

## Work Done
- Fixed 3D building model animations not looping: `active` and `idle` animations played once then stopped
- Root cause: `_on_3d_anim_finished` in `building_logic.gd` only handled `windup→active` and `winddown→idle` transitions, but didn't re-trigger `active` or `idle` when they finished
- Added two cases: replay `active` when still active, replay `idle` when still idle
- Fix applies to all buildings with 3D models, not just drill

## Files Changed
- `buildings/shared/building_logic.gd` — added loop cases to `_on_3d_anim_finished`

## Verification
- Parse check: pass
- Visual test: drill animation loops correctly in-game
